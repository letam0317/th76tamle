.claude/

# Bí mật — TUYỆT ĐỐI không đẩy lên git
.streamlit/secrets.toml
.env
.env.*
!.env.example
*.secret
credentials.json
service-account*.json
.wms-token.json

# Crawler — sản phẩm gỡ lỗi (có thể lộ dữ liệu / phiên đăng nhập)
debug-*.png
debug-*.html
.wms-session/

wms_*.json
ck_*.json
*.xlsx
_*.js
node_modules/
ui_*.png

# Log & lockfile & marker chay ngam theo lich (khong commit)
*.log
*.lock
.last-sync.json
.exports/
# Moc "lan cuoi thay bridge con tuoi" — state CUC BO cua may tram (session-rules.js).
# Do bang mtime nen chep/checkout sang may khac se mang moc sai -> khong commit.
.bridge-thay-cuoi
# Moc "kenh bridge wshr da chung thuc" — cong tac tu dong chuyen luat gio -> luat phien.
# Phai do CHINH may do tu xac nhan, khong duoc chep tu may khac -> khong commit.
.bridge-wshr-ok
# Ban deploy Apps Script co SECRET that — KHONG commit
google-script-DEPLOY.gs

# Backup on dinh (chua .env + GAS secret + task XML) — khong commit
_backup*/

# Repo Pages doc lap (nested git) — khong theo doi trong repo ngoai
factory/
